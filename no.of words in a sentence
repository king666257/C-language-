#include<stdio.h>
#include<string.h>
void main()
{
char para[100];
int c=0;
printf(" enter the sentence :");
gets(para);
for(int i=0;i<strlen(para);i++)

{
    if(para[i]==' ' &&  para[i]!=' ')
    c=c+1;
    printf("%d",c+1);
}
}
